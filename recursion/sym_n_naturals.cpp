#include<iostream>
#include<bits/stdc++.h>
#include<string>

using namespace std;

bool sum(string n)
{
    int len;
    string str=substr(1,len-2);
    len=n.length()
    if (n==n[0])
    return n;

}
int main()
{
    string n;
    cin>>n;
    cout<<sum(n);
    return 0;
}